{
  "name": "hono-realworld",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "prisma:migrate": "bunx prisma migrate dev --name --schema=./src/infrastructure/database/prisma/schema.prisma",
    "prisma:generate": "bunx prisma generate --schema=./src/infrastructure//database/prisma/schema.prisma",
    "prisma:validate": "bunx prisma validate --schema=./src/infrastructure/database/prisma/schema.prisma",
    "lint": "bunx biome check --write"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.2",
    "@types/bun": "latest",
    "prisma": "^5.20.0"
  }
}
